FROM nginx:alpine

# Copy the test files and extension
COPY guacamole-extension-module/src/main/resources/ /usr/share/nginx/html/

# Expose port 80
EXPOSE 80

# Start nginx
CMD ["nginx", "-g", "daemon off;"]